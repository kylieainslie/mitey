name: R-CMD-check

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  R-CMD-check:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        r: ['release']
        include:
          - os: ubuntu-latest
            r: 'devel'
    runs-on: ${{ matrix.os }}
    env:
      R_KEEP_PKG_SOURCE: yes
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:
      # Step 1: Check out the repository
      - name: Checkout repo
        uses: actions/checkout@v4

      # Step 2: Set up R
      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.r }}
          http-user-agent: ${{ matrix.r == 'devel' && 'release' || '' }}

      # Step 3: Set up Pandoc with a specific version
      - name: Setup Pandoc
        uses: r-lib/actions/setup-pandoc@v2
        with:
          pandoc-version: '3.1.1'

      # Step 4: Manual system dependencies installation for Ubuntu
      - name: Install system dependencies on Ubuntu
        if: runner.os == 'Linux'
        run: |
          mkdir -p .github
          echo 'modify:
            sysreqs:
              - from: pandoc-citeproc
                to: ""' > .github/pkg.lock.modify

      # Step 5: Install system dependencies
      - name: Install system dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          use-public-rspm: true
          install-suggests: true
          extra-packages: |
            any::covr
            any::RcppEigen
            any::rcmdcheck
            local::.
          needs: check

      # Step 6: Install package explicitly
      - name: Install package
        run: R CMD INSTALL .

      # Step 7: Install dependencies and run R CMD Check
      - name: Run R CMD check
        uses: r-lib/actions/check-r-package@v2
        with:
          build-vignettes: true
          upload-logs: true

      # Step 8: Upload check results (optional)
      - name: Upload R CMD check results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-${{ matrix.r }}-results
          path: check-results/*.txt
